# ============================================# VoraFind - Flutter / Dart# ============================================# Flutter / Dart generated files.dart_tool/.packages.pub/.pub-cache/build/.flutter-plugins.flutter-plugins-dependencies.metadata# Coveragecoverage/# Generated plugin registrant / localization files**/generated_plugin_registrant.dart# ============================================# Android# ============================================# Gradle.gradle/**/build/# Android Studio / IntelliJ.idea/*.iml# Local Android SDK configurationlocal.properties# Android generated filescaptures/.externalNativeBuild/.cxx/# ============================================# Signing / Secrets# ============================================# Keystores - NEVER commit these*.jks*.keystore*.p12*.pem# Signing configurationkey.properties# Environment / secrets.env.env.*!.env.example# ============================================# iOS# ============================================# Xcode user-specific data**/xcuserdata/*.xcuserstate# Xcode generated filesDerivedData/**/Pods/**/.symlinks/# iOS build artifacts*.app*.ipa*.dSYM*.dSYM.zip# ============================================# macOS# ============================================.DS_Store# ============================================# Windows# ============================================Thumbs.dbDesktop.ini# ============================================# Linux# ============================================*~# ============================================# VS Code# ============================================.vscode/# Keep VS Code project recommendations/settings if# you intentionally add them later.# Remove the line above if you want to commit .vscode/.# ============================================# Temporary / Backup files# ============================================*.tmp*.temp*.log*.bak*.swp*.swo# ============================================# Flutter tool files# ============================================.flutter-plugins.flutter-plugins-dependencies# Generated test artifacts / local sqlite snapshotsvorafind_upgrade_test*.sqlite# Untracked app assets not referenced by pubspec. The bundled embedding model# directory is a committed exception (offline-first): semantic search must keep# working with no network access, so the APK ships it. `assets/*` (not# `assets/`) is ignored so git still descends and the negation applies.assets/*!assets/models/# ============================================# OS / editor leftovers# ============================================*.orig# ============================================# Test artefacts# ============================================# Ephemeral SQLite directories created by database migration tests# (Directory.current.createTempSync leaves these behind when a test run is# interrupted before the finally-block cleanup fires).vorafind_upgrade_test*/vorafind_v2_upgrade_test*/vorafind_db_test*/